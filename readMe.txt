expo init app-name

go to app.json to change the 
 "orientation": "default", 

But if your app is supporting both landscape and portrait else leave it at portrait
install the @react-native-community/hooks package 
for the Dimensions and orientation detection